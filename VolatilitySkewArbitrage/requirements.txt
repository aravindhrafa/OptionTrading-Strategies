# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Options pricing, Greeks, and IV calculations
py_vollib>=1.0.1          # Black-Scholes, Greeks, IV solver
py_vollib_vectorized>=1.0.4   # Vectorized version for chain scanning
mibian>=0.1.3             # Alternative BS implementation

# Market data and earnings calendars
yfinance>=0.2.36          # Historical data + earnings dates
pandas-market-calendars>=4.3.0
earningswhispers>=0.1.0   # Earnings whisper numbers (edge signal)

# Async, networking, websockets
aiohttp>=3.9.0
websockets>=12.0
httpx>=0.26.0

# Broker adapters
tastytrade>=8.0
ib_insync>=0.9.86

# Database for IV crush history
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Monitoring and alerting
structlog>=24.0.0
prometheus-client>=0.19.0
rich>=13.7.0

# Config and validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
PyYAML>=6.0

# Data storage
pyarrow>=14.0.0           # Efficient IV history storage

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Dev tools
black>=24.0.0
isort>=5.13.0
mypy>=1.8.0
